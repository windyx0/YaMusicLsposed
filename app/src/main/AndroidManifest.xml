<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="YaMusic Downloader (LSPosed)"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Add download button to Yandex Music player" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedauthor"
            android:value="windyx0" />
            
        <provider
            android:name=".PrefsProvider"
            android:authorities="com.windyx0.yamusiclsposed.prefs"
            android:exported="true" />
            
    </application>

</manifest>
